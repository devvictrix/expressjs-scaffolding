/**
 * An enum representing the possible Node.js environments.
 * Using an enum prevents typos and provides autocompletion.
 */
export enum NodeEnv {
    Development = 'development',
    Production = 'production',
    Test = 'test',
}